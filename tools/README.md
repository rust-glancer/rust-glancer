# Agentic tools

This folder contains tools written by agents for agents.
The goal is to enable agents to do safe actions without having to explicitly approve each
command if, say, they need a custom rust log env var.

